# 乐高画作智能转换器 —— 技术方案与架构设计 (Technical Design Doc)

> 版本：V1.0（开发前基线）
> 输入依据：《乐高生图项目文档.md》V1.0 + 《乐高生图项目_需求决策记录.md》（决策 D1–D16）
> 适用对象：核心算法库 / PyQt6 桌面端 / 数据层 / 导出层 的开发人员
> 本文档回答："系统怎么拆、数据怎么流、算法怎么做、性能怎么达标、目录结构长什么样"。

---

## 1. 设计目标与非目标

### 1.1 目标
- **可复用核心**：`legoart` 核心包零 UI 依赖（不 import PyQt6），未来 Web 端（FastAPI）直接复用同一套 API。
- **离线可用**：除"全量目录导入"外，首版所有功能在无网络环境可用；模型权重随包提供（体积受限的用下载脚本）。
- **性能达标**：48×48（2304 格）量级本地 ≤60s（目标实际 ≤10s 主循环）。
- **数据可追溯**：方案、BOM、库存扣减均带出处；历史项目可回放（project_history 快照）。
- **授权干净**：代码 GPL v3/MIT（免商）；内嵌数据仅限授权允许的部分，其余由用户侧导入（D15/O11）。

### 1.2 非目标（首版明确不做）
云端计算、纹理保留风格、拆除/回写、"大面积低像素"特殊诉求（D3 备注）、在线零件图片（D16）。

---

## 2. 总体架构（分层）

```
┌─────────────────────────────────────────────────────────────┐
│  UI 层（PyQt6 桌面端 legoart_desktop）                        │
│  页面编排 · 画布预览/裁剪 · 参数表单 · 目录选择器 · 导出入口      │
└───────────────┬─────────────────────────────────────────────┘
                │ 仅通过 Core Facade 的领域 API 交互
┌───────────────▼─────────────────────────────────────────────┐
│  核心算法库 legoart（零 UI 依赖，未来 Web 端同此复用）           │
│  ├─ pipeline : 端到端编排（ImageSpec→MosaicPlan→BOM）          │
│  ├─ color    : CIEDE2000 · 色库装载 · 最近色匹配(向量化)        │
│  ├─ mosaic   : 采样/量化 · 同色大板合并 · 显著性凸起            │
│  ├─ inventory: 理想求解 · 库存约束三策略 · 扣减                 │
│  ├─ detect   : 拍照识别适配器（YOLO/颜色分割，可插拔）           │
│  ├─ export   : Excel BOM · PDF 说明书(分层6视图) · 渲染         │
│  ├─ catalog  : 精选目录装载 · Rebrickable 导入器                │
│  └─ storage  : SQLite（catalog/inventory/history）DAO          │
└───────────────┬─────────────────────────────────────────────┘
                │ 读写
┌───────────────▼─────────────────────────────────────────────┐
│  本地存储 data/                                               │
│  SQLite 库 · 精选目录 JSON · 模型权重 · 导出目录 · 日志         │
└─────────────────────────────────────────────────────────────┘
```

**分层规则**：
1. UI → 核心：只调用 `legoart.api` 暴露的高层函数/领域对象，不直接碰 SQL、不直接 import 算法内部模块；
2. 核心内部：`pipeline` 编排 → `color/mosaic/inventory` 纯计算（无副作用）→ `storage/export` 带副作用，计算与 IO 分离便于测试与未来并发；
3. 核心包内所有对外错误用统一 `LegoArtError` 家族抛出，UI 捕获后本地化文案提示。

---

## 3. 技术选型

| 用途 | 选型 | 版本策略 | 说明 |
| :-- | :-- | :-- | :-- |
| 语言/运行时 | Python | 3.13.5（已装） | |
| 桌面 UI | PyQt6 | ≥6.7（待安装） | Qt6，中英 i18n 用 Qt Linguist |
| 数值/图像 | numpy + Pillow | 已装 | 主循环向量化 |
| 图像处理 | opencv-python | 待安装 | 裁剪/缩放/边缘/形态学/轮廓 |
| 显著性模型 | Torch（已装 2.12.1） | — | U²-Net 轻量版（u2netp）离线权重，抽象为 `SaliencyModel` 可替换 |
| 零件检测 | Ultralytics YOLO | 待确认 3.13 兼容性 | 拍照识别：实例分割/检测 + 颜色聚类；或先用 opencv 色块分割做 MVP 兜底 |
| Excel | openpyxl | 已装 | |
| PDF | ReportLab | 待安装 | 矢量绘图原生支持 |
| 数据库 | sqlite3（标准库） | — | 单文件，事务开启 |
| 序列化 | JSON/msgpack（可选） | — | 快照/导入导出 |
| 测试 | pytest | 待装 | 算法单测 + 冒烟 |
| 打包（后续） | PyInstaller | 后续定 | |

**Python 3.13 兼容性风险**：`ultralytics`、`PyQt6`、`opencv-python`、`reportlab` 均需在脚手架阶段装好并跑通最小冒烟；若 ultralytics 与 3.13 冲突，检测模块降级为"opencv 颜色分割候选 + 用户确认"并保留 YOLO 适配器接口（O10 时再定模型来源）。

---

## 4. 端到端数据流

```
导入图片 ──裁剪(自由框，锁定比例)──> CroppedImage
   │ 输入 宽(studs|cm)  → 按比例自动高 (D4)
   ▼
ImageSpec{ grid_w, grid_h, style=色块化, mode=理想|库存, shortage=..., saliency_on }
   ▼
[1] 网格采样：源图 → (H×W) 平均色矩阵（Numpy resize/binning，向量化）
   ▼
[2] 颜色量化：逐格 CIEDE2000 → 最近官方色（候选色集可配置：全色库/常用色集）
       ↓ (记录 ΔE00 残差，供"纹理保留"二期复用/超阈值报警)
   ▼
[3] 色块网格 GridModel{ w,h, cell_color }   ←─ 用户可在此步前/后叠加手动改色
   ▼
[4] 凸起区域（可选，D6）：
     显著性图 → 阈值→连通域 → 与用户手绘增删合并
     → RegionMask[]，每区域分配层高（默认规则：saliency score 分档 1–3 层）
   ▼
[5] 分层结构 LayerStackModel：
     layer0 = 底板层(可选，见 O8)
     按区域层高把网格叠加：凸起区在第 n 层以上继续填色（上表面色=主色）
     → 每层得到 {level: int, grid: 色格矩阵}
   ▼
[6] 同色大板合并（每层独立执行，贪心最大矩形，见 §6.4）
     → 每层 PartPlacement[]（design_id + 锚点 + 朝向）
   ▼
[7] 需求轴求解（见 §7）
     理想模式：直接按合并结果出购物清单
     库存约束：按策略求解 → (可拼部分, 替代清单|缺件清单|极限结果)
   ▼
[8] 汇总 → BomModel{ items[] }  其中 item = {layer, design_id, color_id, qty}
   ▼
[9] 导出：Excel BOM（openpyxl） + PDF 说明书（ReportLab，分层6视图）
       + 确认拼搭 → 库存扣减（事务）+ project_history 快照入库
```

所有中间产物（第 3–8 步）均为纯数据对象，**同一份数据同时驱动：预览画布、Excel、PDF、库存扣减**——避免各输出端各自算一遍导致不一致。

---

## 5. 核心领域模型（dataclasses / pydantic 风格纯数据）

```python
# legoart/model/
ImageSpec      # grid_w, grid_h, style: ColorBlock|(TextureReserved), use_inventory: bool,
               # shortage_strategy: Approx|MissingList|Extreme, saliency_enabled, color_set: str
GridModel      # width:int, height:int, colors: np.ndarray (str 色key) 或 {row, col: color_id}
SaliencyMap    # score: np.ndarray (H×W 0..1), threshold, regions: list[Region]
Region         # id, mask(或 bbox+polygon), score, raise_layers:int, source: ai|user
LayerStack     # layers: list[Layer]; 每层 level:int + GridModel；含底板信息
PartPlacement  # design_id:str, x:int, y:int, w:int, h:int, color_id:str, layer:int
BomItem        # layer:int|None(合并行), design_id, color_id, qty:int
BomModel       # items:list[BomItem]; 附 色名/色号/名称 展示快照（防目录后续变更）
InventoryHit   # design_id,color_id, requested, allocated, substitute_of:设计号|None
MosaicPlan     # 端到端结果：image_spec, grid, regions, layer_stack,
               # placements_by_layer, bom, warnings[], elapsed_ms
ProjectRecord  # id, created_at, params_json, plan_snapshot_json(含 BOM)
```

约定：**色一律以字符串 key 引用**（BrickLink 色名作为主 key，如 `Red`/`Bright Red`），RGB 从色库装载，避免到处拷贝数值；BOM/快照写入时冗余色名色号等展示字段（D15 快照一致性）。

---

## 6. 关键算法设计

### 6.1 色库与最近色匹配（CIEDE2000，向量化）
- 色库：`catalog/colors.json`（精选，字段见 §9.2）；装载为 `Palette`（numpy float 数组 + key→RGB/Lab 索引）。
- 匹配：sRGB→Lab（D65）后，对 N 个网格色 × M 个候选色做 **CIEDE2000 距离矩阵**。直接 CIE76 向量化很快（2304×~120 = 27 万对），但需求指定 CIEDE2000；实现方案：
  1. 用 `colour-science`（若许可合适）或自实现标准 CIEDE2000（公开公式）；
  2. 向量化技巧：先按 CIE76 取每行 top-K（如 K=5）粗筛，再对粗筛子集算精确 ΔE00，把全量精确计算从 27 万对降到 ~1 万对级。
- 输出每格最近色 key + 残差 ΔE00；残差过高（如 >8）时聚合为 warning 提示用户（如"大面积深色区无可匹配色，建议更换输入或放大尺寸"）。

### 6.2 网格采样（D3：尺寸=分辨率）
- 源裁剪图按目标网格 (H×W) 用面积平均（Pillow `resize(BOX)` 或 numpy block-mean）得到每格代表色；格内再做可选的颜色域降噪（3×3 中值作用于格矩阵），抑制单格椒盐噪色。
- cm 输入换算：1 stud ≈ 8mm；`cm→studs = round(cm*10/8)`，换算结果向用户回显对应 studs 数（D10）。

### 6.3 显著性凸起（D6，离线）
- 模型：U²-Netp（轻量）TorchScript/ONNX 权重，随包或首次运行下载；抽象接口 `SaliencyModel.predict(rgb)->score_map`，便于将来换 BASNet 等。
- 后处理：score 归一化 → 阈值分档 → 形态学开闭 → 连通域 → `Region(score, mask)`；UI 支持手绘增/删/改区域与高度（1–N 层，O7 默认"置信度分档 1–3 层"，高度可全局覆盖）。
- 纯计算（无模型）路径：模型文件缺失时自动降级为"无凸起 + 提示"，不阻塞主流程。

### 6.4 同色大板合并（D5，每层独立）
- 输入：该层色格矩阵。目标：把同色连通区域切成尽可能大的矩形板。
- 贪心最大矩形：逐行扫描同色运行段 → 纵向扩展为候选矩形（运行长度编码），按面积降序放置并标记占用；板型白名单（1×1,1×2,1×3,1×4,1×6,1×8, 2×2,2×3,2×4,2×6,2×8 …），优先取面积最大的合法板型。
- 输出：该层 `PartPlacement[]` + 该层"最大/平均板面积"统计（可作色块化质量指标回显）。
- 复选规则：同色但被凸起/空洞分隔 → 按连通域分别处理，保证同层不重叠、无缝隙、无悬空格。

### 6.5 分层构造（凸起 → 物理层）
- 底板层（level 0）为整幅底板（若有，见 O8）；凸起区域 level≥1 各层只含该区格子的板（上表面主色，侧壁同色板）。
- "层"= 真实物理层：用户按说明书自下而上逐层拼（对应 PDF 每层 6 视图，D9）。无凸起时方案即单层（官方 Art 风）。

---

## 7. 需求轴求解（inventory 模块）

统一输入：目标 = 各 (design_id, color_id, qty) 的**未合并语义单位**（合并后的板件粒度）逐项消费。

- **理想模式**：全量目标直接入购物清单（无库存参与）。
- **库存约束**（先按 (design_id, color_id) 精确匹配，再按同面积替代规则（如 1×4→2×1×2、1×4→1×2×2，见原文档 §8-2））：
  - 策略 A 近似替代：精确+替代都缺 → 在库存中找"形状可替代且颜色最接近（ΔE00 最小）"的板，替换并记录损失；
  - 策略 B 缺件清单：保留原色方案，缺的 (design_id,color_id,qty) 汇总为缺件清单（可再按目录/全量目录解析为设计号导出/下单）；
  - 策略 C 极限拼搭：缺啥都不替代，能拼多少拼多少，无法匹配处留空或黑白灰填充（仅输出可完成部分），结果显著降级时提示用户。
- **结果对象**：`SolverResult{ placements(可拼), missing[], substituted[], warnings[] }`，三种策略结果统一结构，预览层可同画布渲染。
- **扣减**：仅在 UI"确认开始拼搭"后执行（原文档 §2.4），同一事务内写 inventory 扣减 + project_history 快照；方案/预览阶段绝不写库存。

---

## 8. 存储设计（SQLite，单文件 userdata.db）

### 8.1 用户库（runtime DB）
```sql
-- 库存（手动/Excel/拍照三种来源 source 字段区分）
CREATE TABLE inventory (
  id INTEGER PRIMARY KEY,
  design_id  TEXT NOT NULL,          -- 目录主键（如 3024）
  color_id   TEXT NOT NULL,          -- 色主 key（BrickLink 色名）
  quantity   INTEGER NOT NULL DEFAULT 0,
  source     TEXT NOT NULL DEFAULT 'manual', -- manual|excel|photo
  note       TEXT,
  updated_at TEXT NOT NULL
);
CREATE UNIQUE INDEX ux_inv ON inventory(design_id, color_id);

-- 项目历史
CREATE TABLE project_history (
  id INTEGER PRIMARY KEY,
  created_at TEXT NOT NULL,
  params_json TEXT NOT NULL,         -- ImageSpec + 目录版本号
  plan_snapshot_json TEXT NOT NULL,  -- LayerStack+BOM 全量快照（可回放/重导出）
  bom_excel_path TEXT, pdf_path TEXT
);

-- 目录（精选内置；全量导入后同表扩充/覆盖）
CREATE TABLE catalog_parts(design_id TEXT PRIMARY KEY, name TEXT, category TEXT,
  stud_w INT, stud_h INT, shape_family TEXT, -- plate|brick|tile|...
  extra TEXT);
CREATE TABLE catalog_colors(color_id TEXT PRIMARY KEY, name_bl TEXT, name_tlg TEXT,
  rgb TEXT, is_trans INT, material TEXT, recommended INT);
CREATE TABLE catalog_elements(design_id TEXT, color_id TEXT, PRIMARY KEY(design_id,color_id));
```
> 与原文档表结构差异说明：原 `inventory(id,color_code,part_shape,quantity)` 升级为引用目录主键（design_id + color_id）以支撑真实件号（D15）；`project_history` 保留并扩展快照。若需要与原文档严格一致的可视化列，可用视图补 `color_code/part_shape` 别名。

### 8.2 目录版本与升级
- 每个 DB 记录 `meta(key,value)`：`catalog_version`、`app_version`；方案快照里记 catalog 版本，保证历史重导出时颜色/件名一致。

---

## 9. 数据资产与授权（data/）

### 9.1 布局
```
data/
├─ catalog/curated/          # 精选内置目录（随包，MIT 自维护 + 数据来源声明）
│   ├─ colors.json           # 色库（Rebrickable 结构 + legocolors 校准字段）
│   ├─ parts_plates.json     # 方案用板件（1×1~2×8 … 含 design_id/studs 尺寸/名称）
│   └─ parts_common.json     # 常见录入件（板/砖/瓦/斜坡常用件）
├─ importers/                # 用户侧全量导入输出目录（不随包分发）
├─ models/                   # 模型权重（u2netp / yolo .pt；gitignore，脚本下载）
└─ notices/THIRD_PARTY_NOTICES.md  # 所有第三方数据/模型许可与署名（O11 逐项落实）
```

### 9.2 色库字段（colors.json 条目示例）
```json
{"color_id":"Bright Red","name_tlg":"Bright Red","name_bl":"Red",
 "rgb":"#C91A09","material":"solid","recommended":1,
 "refs":{"rebrickable":4,"ldraw":4,"tlg_code":21}}
```
- 精选集默认含常用实色约 40–60 色（含 recommended 高优先级），透明/珍珠/电镀等按需再加，UI 提供"仅常用色/全部颜色"候选切换（§6.1 color_set）。
- 全量导入后 colors 以 Rebrickable 为准、legocolors 仅交叉校验（记录差值日志，不覆盖）。

### 9.3 目录装载
- 启动顺序：内置精选 JSON 装入内存 catalog → 若 DB 存在"全量导入"版本则以 DB 为准（版本号比较）→ UI 只面对统一 `Catalog` 查询接口。

---

## 10. 导出设计

### 10.1 Excel BOM（openpyxl）
- Sheet「物料清单」列：`层(Layer)`、`零件形态(Part)`、`设计号(Design ID)`、`颜色名称/色号(Color Name/ID)`、`RGB`、`单层用量(Qty/Layer)`、`总用量(Total)`（与原文档 §2.5 字段一致并扩展设计号/RGB/层）。
- Sheet「方案参数」：原图路径、网格 W×H、风格/模式/策略、凸起区域数与总层数、生成耗时、目录版本。
- Sheet「缺件清单」（策略 B 或替代明细（策略 A）时出现）。
- 样式：表头冻结、列宽自适应、色块单元格填充 RGB 预览。

### 10.2 PDF 说明书（ReportLab，分层 6 视图）
- 结构：封面（成品预览渲染图 + 标题/参数）→ 物料清单页 → **逐层搭建页**：对层 k，展示"已拼到第 k 层"模型在 **正/背/左/右/俯/仰** 六视角的示意图 + 该层新增零件清单。
- 6 视图计算：俯/仰=色格矩阵（含底板）；正/背/左/右=按层列高度扫描投影：每个格子取"从该方向可见的最高层颜色"做条带图（矢量，不用位图）；色块以 Catalog RGB 填充、格线浅灰。
- 渲染均基于 §5 领域对象（placements/color），一套函数出图，可复用于 UI 预览与 PDF（O9 版式细节后续定稿，矢量线条优先）。
- 中文支持：ReportLab 注册中文字体（随包带思源黑体子集或系统字体回退），并在 PDF 内嵌字符子集（i18n 见 §11）。

---

## 11. 国际化与错误处理
- 文案统一走 `legoart/i18n/`：核心包用 Python dict 双语文案（不引入 Qt 依赖），UI 层用 Qt `tr` + `.ts/.qm`。核心抛出的错误码（如 `ERR_COLOR_NO_MATCH`、`ERR_OVER_60S`、`ERR_MODEL_MISSING`）由 UI 映射为本地化提示。
- 单位/数字格式按 locale 处理；PDF/Excel 表头按当前语言生成。

---

## 12. 性能预算（D11）
- 48×48=2304 格：采样 resize + 格中值 <0.5s；CIEDE2000 粗筛+精算 <1s；大板合并 <1s；显著性推断（CPU u2netp）约 2–8s（一次性，可缓存）；PDF/Excel 生成 <5s。
- 全流程 CPU 目标 ≤15s（硬上限 60s）。超时以耗时预算中断并给出"降分辨率/仅理想模式"建议（用 `Signal`/进度回调从 pipeline 上报 UI，核心不 sleep）。
- 大网格（>64 宽）时对显著性先降采样到 ≤256 边长做推断，结果上采样回网格。

---

## 13. 目录结构（落地版）

```
乐高生图项目/
├─ docs/
│  ├─ 乐高生图项目文档.md
│  ├─ 乐高生图项目_需求决策记录.md
│  └─ 技术方案与架构设计.md          # 本文件
├─ src/
│  ├─ legoart/                        # 核心算法库（纯 Python，无 UI）
│  │  ├─ api.py                       # Core Facade：generate_plan()/solve()/export_*()
│  │  ├─ errors.py                    # LegoArtError 家族 + 错误码
│  │  ├─ model/                       # §5 领域对象（dataclass + to/from dict）
│  │  │  ├─ image_spec.py  grid.py  regions.py  layers.py
│  │  │  ├─ placements.py  bom.py  plan.py
│  │  │  └─ __init__.py
│  │  ├─ color/
│  │  │  ├─ ciede2000.py              # ΔE00（粗筛+精算）
│  │  │  ├─ palette.py                # Palette 装载/查找/Lab 缓存
│  │  │  └─ __init__.py
│  │  ├─ mosaic/
│  │  │  ├─ sampling.py               # 面积平均采样 + 格矩阵中值降噪
│  │  │  ├─ quantize.py               # 逐格最近色（含候选色集）
│  │  │  ├─ merge_plates.py           # §6.4 同色大板合并
│  │  │  ├─ relief.py                 # 凸起区域 → 分层结构
│  │  │  └─ __init__.py
│  │  ├─ saliency/
│  │  │  ├─ base.py                   # SaliencyModel 抽象
│  │  │  ├─ u2net.py                  # U²-Netp 推理（torch）
│  │  │  └─ __init__.py
│  │  ├─ inventory/
│  │  │  ├─ ideal.py                  # 理想购物车
│  │  │  ├─ constrained.py            # 库存约束三策略 + 替代规则
│  │  │  ├─ substitute.py             # 面积替代表（1×4→2×1×2 …）
│  │  │  └─ __init__.py
│  │  ├─ detect/                      # 拍照识别（可插拔，O10 细化）
│  │  │  ├─ base.py                   # PhotoDetector 抽象
│  │  │  ├─ opencv_seg.py             # 颜色分割兜底（MVP 可用）
│  │  │  ├─ yolo.py                   # Ultralytics YOLO 适配器
│  │  │  └─ __init__.py
│  │  ├─ catalog/
│  │  │  ├─ loader.py                 # 内置 JSON/DB 装载，统一 Catalog 接口
│  │  │  ├─ curated.py                # 精选集 schema 校验
│  │  │  └─ __init__.py
│  │  ├─ export/
│  │  │  ├─ excel.py                  # openpyxl BOM
│  │  │  ├─ pdf.py                    # ReportLab 说明书编排
│  │  │  ├─ views6.py                 # 6 视角正交投影计算/绘制
│  │  │  ├─ fonts.py                  # 中文字体注册
│  │  │  └─ __init__.py
│  │  ├─ storage/
│  │  │  ├─ db.py                     # 连接/迁移/meta
│  │  │  ├─ inventory_dao.py  history_dao.py  catalog_dao.py
│  │  │  └─ __init__.py
│  │  ├─ pipeline.py                  # §4 编排（进度回调、耗时预算）
│  │  ├─ i18n/                        # 核心包文案（zh/en dict）
│  │  └─ __init__.py
│  └─ legoart_desktop/                # PyQt6 桌面端
│     ├─ __main__.py  app.py  main_window.py
│     ├─ pages/                       # 向导页
│     │  ├─ page_import.py            # 导入+裁剪（含比例锁定）
│     │  ├─ page_size.py              # 宽(studs/cm)+模式/策略/风格 参数
│     │  ├─ page_preview.py           # 预览画布：改色/凸起增删/3D 简视
│     │  ├─ page_inventory.py         # 库存 CRUD/Excel/拍照
│     │  └─ page_output.py            # 导出 Excel/PDF/确认拼搭扣减
│     ├─ widgets/
│     │  ├─ canvas.py                 # 网格画布(缩放/取色/笔刷)
│     │  ├─ part_picker.py            # 目录选择器(搜索+分类+自绘示意 D16)
│     │  ├─ color_swatch.py  inv_table.py
│     ├─ services/                    # UI↔api 适配（进度信号/工作线程）
│     ├─ resources/                   # qss/icons/translations(.ts/.qm)
│     └─ __init__.py
├─ scripts/
│  ├─ import_rebrickable_csv.py       # 全量导入（用户下载后运行，D15）
│  ├─ build_curated_catalog.py        # 精选 JSON 生成/校验（开发期）
│  ├─ fetch_models.py                 # 下载 u2netp/yolo 权重到 data/models
│  └─ migrate_db.py
├─ data/                              # 见 §9.1（models/ 等 gitignore 项除外）
├─ tests/
│  ├─ unit/                           # ciede2000/采样/合并/求解/DAO
│  ├─ fixtures/                       # 合成测试图/色板/目录子集
│  └─ smoke/                          # pipeline 端到端 + 导出冒烟
├─ requirements.txt  requirements-dev.txt
├─ pyproject.toml                     # 包元数据 + 打包配置
├─ README.md  LICENSE  .gitignore
└─ third_party/                       # 第三方许可全文（CC BY 2.0 等）
```

---

## 14. 测试与验收要点
- 单元：CIEDE2000 与参考实现比对（公开样例对 ΔE00<1e-6）；合并算法在合成"棋盘/条纹/孤岛"图上验证无重叠无缝隙；三种库存策略在构造库存上断言结果；DAO 事务与扣减原子性。
- 冒烟：48×48 合成油画图全流程（含凸起+导出）计时断言 ≤60s；无模型文件时降级路径可走通。
- 视觉：导出 PDF 打开抽检 6 视图方向正确；Excel 打开校验列与填充色。
- 验收对齐：D1–D16 每条可测映射（例：D4 高=ceil(w/aspect)；D11 计时；D12 三条录入来源均可入库；D15 导入后设计号可搜）。

---

## 15. 开发里程碑（映射到模块，落地顺序）
| 阶段 | 内容 | 交付物 |
| :-- | :-- | :-- |
| M0 脚手架 | pyproject/requirements/包骨架/CIEDE2000+色库装载+单测；装齐 PyQt6/opencv/reportlab 冒烟 | 可 import 的核心包 + 冒烟绿 |
| M1 色块化链路 | 采样→量化→网格模型 + CLI 冒烟（忽略 UI） | `pipeline` 输出 GridModel/BOM JSON |
| M2 合并+凸起 | merge_plates + relief + saliency 权重脚本 | 分层 LayerStack + placements |
| M3 PyQt6 壳 | 页面骨架 + 导入/裁剪/参数/预览画布 | 可运行的 MVP UI（无导出） |
| M4 库存 | DAO/CRUD/Excel 导入导出/约束三策略/扣减 | 库存页面 + 求解结果预览 |
| M5 导出 | Excel + PDF(6视图) + 字体 | 双导出文件验收 |
| M6 拍照识别 | detect 适配器（opencv 兜底→YOLO）+ 批量入库向导 | 拍照入库流程可用 |
| M7 打磨 | i18n 完善、目录导入器、性能/打包 PyInstaller | 首版发布包 |

---

## 16. 主要风险与对策
| 风险 | 影响 | 对策 |
| :-- | :-- | :-- |
| Python 3.13 下 ultralytics/YOLO 兼容 | M6 延迟 | 先 opencv 颜色分割兜底；YOLO 接口隔离，验证通过再切 |
| CIEDE2000 向量化性能 | 大网格超时 | CIE76 粗筛 + top-K 精算（§6.1） |
| 显著性模型体积/速度 | 安装包大/慢 | u2netp 轻量 + 降采样推断 + 模型缺失降级路径 |
| 大板合并质量（艺术感差/缝） | 体验 | 质量指标回显 + 预览改色/刷子；后续可加代价函数调优 |
| 全量目录授权/体积 | 法务/包体 | 用户侧导入（D15/O11），通知文件集中管理 |
| 6 视图表达晦涩 | 说明书难懂 | 先实现俯视+四侧投影矢量图，O9 版式评审 |
| 目录/色库变更导致历史 BOM 漂移 | 一致性 | 快照冗余展示字段 + catalog 版本号（§8.2） |
