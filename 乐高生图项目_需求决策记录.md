# 乐高画作智能转换器 —— 需求澄清与决策记录

> 本文档是《乐高生图项目文档.md》(V1.0) 的补充决策记录，记录与需求方逐条确认后的**修正口径**。
> 规则优先级：本文件 > 原始需求文档；如两者冲突，以本文件为准。开发中新增的口径确认持续追加到本文件。

**最近确认日期**：2026-09-03
**本机环境**：Windows + Python 3.13.5（numpy/pillow/pandas/openpyxl/torch 已装；opencv-python、PyQt6、reportlab 待安装）；Node v24.9.0、Git 2.53 可用。

---

## 1. 决策总表

| # | 议题 | 确认结论 |
| :-- | :-- | :-- |
| D1 | 首版交付形态 | **纯 Python 核心算法库 + PyQt6 桌面客户端 MVP**（算法与 UI 分层，算法包可被后续 Web 端复用） |
| D2 | 选项轴关系 | **风格轴（色块化 / 纹理保留）× 需求轴（理想购物车 / 库存约束）正交**，UI 上独立选择、可任意组合；「油画 / 照片」仅作为提示默认风格的辅助信息，不是一个独立功能开关 |
| D3 | 尺寸与细节等级 | **两者是同一件事**：用户指定目标长×宽（studs，或 cm 换算成最接近的 studs），W×H 分辨率本身就决定了细节等级——网格越大细节越精细。**没有独立的“细节等级”滑块**。“大面积但低像素”的特殊诉求后续再优化 |
| D4 | 宽高比处理 | 用户先对原图裁剪；**成品宽高比 = 裁剪后图像的宽高比（用户不可另设比例）**。用户只输入一个维度（如只输入宽），程序按裁剪图比例自动算出另一个维度 |
| D5 | 颗粒/零件形态 | 色块单元**自动把相邻同色格合并成大板**（1×1 / 1×2 / 2×2 / 2×4 等矩形板，需矩形贪心切分算法），而非全部 1×1 |
| D6 | 成品结构与凹凸 | **首版就含“AI 重点识别 + 凸起”**：AI（显著性）自动标记画面重点区域，用户可在预览中微调；重点区域做凸起（垫高多层板） |
| D7 | 需求轴首版范围 | **首版即做双模式**：理想购物车 + 库存约束（含三种缺货策略） |
| D8 | 风格轴首版范围 | **首版先做“色块化”**；“纹理保留（油画）”作为紧随其后的第二迭代 |
| D9 | 凹凸的交付方式 | **凸起 + 分层 PDF 说明书一起进首版**：因为凸起使成品有多个物理层，必须有分层（每层 6 视图：正/背/左/右/俯/仰）说明书才能指导拼搭 → **PDF 生成提前到首版交付** |
| D10 | 尺寸单位换算 | 1 stud ≈ 8mm（标准乐高颗粒节距）；cm ↔ studs 换算需向用户提供预览说明 |
| D11 | 性能约束 | 本地主循环 Numpy 向量化；默认 48×48（2304 片级）应在 60s 内完成；超时提示转云端或降分辨率 |
| D12 | 首版库存来源 | **手动录入 + Excel 导入/导出 + 拍照识别（YOLO 颜色识别、零件形态由用户下拉确认）全部进首版** |
| D13 | 缺货策略 | **三种策略（近似替代 / 生成缺件清单 / 极限拼搭）首版全部实现**，生成时用户三选一 |
| D14 | 云端模式 | **首版纯本地**，不做云端入口；算法包与 UI 分层为将来封 FastAPI 留好边界 |
| D15 | 目录数据策略 | **精选内嵌（离线可用）+ 可选全量导入**：方案用板件与常见录入件做自维护精选表；全量目录由用户自行从 Rebrickable 下载官方 CSV 导入（parts/colors/elements/categories），BOM/缺件清单带真实乐高设计号；数据授权责任在用户侧。颜色表：Rebrickable `colors` 主表（BrickLink 色名/TLG 色号/RGB/material）+ legocolors 交叉校准（CIEDE2000 基准） |
| D16 | 零件选择器 | **文本（设计号+名称+分类）+ 自绘示意图（矩形轮廓/色块）**，完全离线、无图片版权风险；不做在线拉图 |

## 2. 由此推出的首版（MVP）功能清单

1. 图片导入（JPG/PNG/BMP/WEBP 等）与**自由裁剪**（含按裁剪比例预览成品边界）。
2. 分辨率输入：用户输入宽度（studs 或 cm），按裁剪比例自动算高度。
3. 色块化核心算法：采样 → 主色提取 → **CIEDE2000 最近官方色匹配** → 相邻同色合并成大板（输出逐格网格 + 合并后零件清单）。
4. 需求轴双模式：
   - **理想购物车**：从官方全量色库取最接近色，输出“需购买零件清单”；
   - **库存约束**：读库存，优先用库存，缺货时执行用户选择的缺货策略（三种见下）。
5. **AI 重点识别（显著性）→ 凸起区域** + 用户在预览中的手动增删/调整；凸起以“该区域叠加板层”实现。
6. 分层结构模型（自下而上的物理层），每层含格级图案数据。
7. 输出一：
   - **Excel 物料清单**：颜色名称/色号、零件形态（合并后的大板形态）、单层用量、总用量；
8. 输出二：
   - **PDF 拼搭说明书**：成品预览 + 物料清单 + **每层 6 视图**（正/背/左/右/俯/仰）拼搭图。
9. SQLite：`inventory` 与 `project_history` 表（首版至少支撑库存约束模式与历史快照）。
10. 库存管理界面：手动增删改、Excel 导入/导出、**拍照批量识别**（YOLO 识别颜色边界 + 用户下拉框确认零件形态后批量入库）。
11. 生成方案后按需（确认拼搭后）自动扣减库存。
12. 界面中英文切换。

## 2b. 首版不包含（明确排除）

- 云端计算模式（纯本地）。
- “纹理保留”风格（第二迭代）。
- 拆除/回写逻辑（不涉及“拆除之前的作品”）。

## 3. 与原始文档的差异点（需要留意）

- 原文 2.2 “细节等级滑块” → 取消（D3）。
- 原文 2.2 “长 x 宽自由指定、用户也可选 cm” → 简化为“宽高比锁定裁剪图 + 输入单维度自动补全”（D4），但仍保留 studs/cm 双单位输入。
- 原文 Phase 1 仅 Excel、Phase 2 才做 PDF → PDF 提前进首版（D9）。
- 原文 Phase 1 “忽略库存” → 库存约束进首版（D7），但视觉识别（YOLO 拍照录入）仍可后置。
- 原文把“凹凸”列为整体特征/注意项 → 明确为首版功能（D6）。

## 4. 尚未最终确认 / 开发前需定稿的开放项（Open Items）

- ~~O1 首版库存来源~~ → **已定 D12：手动 + Excel + 拍照识别全进首版**（仍需定：YOLO 模型选择、识别流程细节）
- ~~O2 三种缺货策略~~ → **已定 D13：三种全部实现**
- ~~O3 云端计算~~ → **已定 D14：首版纯本地**
- ~~O6 BOM 零件形态/设计号~~ → **已定 D15 + D16：内嵌精选目录 + 可选 Rebrickable 全量导入，BOM 带设计号；选择器文本+自绘示意图**
- O4 “纹理保留”第二迭代的算法方向（边缘检测增强 / 抖动 / 更多色深等）——后续单独确认。
- O5 乐高色库数据来源与授权（legocolors JSON）的具体落地文件格式与版本。
- O7 凸起的实现细节：凸起高度由谁决定（AI 置信度？用户指定层数？），每处凸起 1~N 层。
- O8 成品超过单块底板（如 48×48）时的底板拼接/分页说明书规则。
- O9 PDF 说明书版式与 6 视图绘制风格（矢量线条 / 彩色网格）。
- O10 拍照识别的具体交互：一次拍几张、需要怎样的拍摄条件、与“散落积木”文档描述的贴合程度。
- O11 数据来源授权落实：引入 Rebrickable CSV / legocolors / LDraw 数据时的具体许可证文本、署名页与数据文件存放方式（开发到数据层时逐一定稿）。
