[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "legoart"
version = "0.1.0"
description = "乐高画作智能转换器 - convert pictures into LEGO mosaic building plans (core algorithm library)"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11"
authors = [{ name = "legoart contributors" }]
keywords = ["lego", "mosaic", "pixel-art", "image-processing", "ciede2000", "pyqt6"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: End Users/Desktop",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Multimedia :: Graphics",
    "Topic :: Scientific/Engineering :: Image Processing",
]
dependencies = [
    "numpy>=2.0",
    "Pillow>=11",
    "opencv-python>=4.10",
    "openpyxl>=3.1",
    "reportlab>=4.2",
]

[project.urls]
Homepage = "https://github.com/Ray0612/legoart"
Repository = "https://github.com/Ray0612/legoart"
Issues = "https://github.com/Ray0612/legoart/issues"

[project.optional-dependencies]
desktop = ["PyQt6>=6.7"]
# torch is intentionally NOT declared here (heavy). Enable saliency/detection by:
#   pip install -e ".[ml]"   -> installs torch + torchvision (choose CPU wheel on Windows)
ml = ["torch>=2.2", "torchvision>=0.17"]
dev = ["pytest>=8", "ruff>=0.6"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 120
target-version = "py311"

# 固定规则集（不随 ruff 版本默认漂移），只启用高置信度的问题规则。
[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]

[tool.ruff.lint.per-file-ignores]
# 包的 __init__ 常做再导出（re-export），允许 F401
"__init__.py" = ["F401"]
"tests/**" = ["F401", "F841"]
"scripts/**" = ["F401", "F841", "F541"]
